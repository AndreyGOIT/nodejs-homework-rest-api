const express = require("express");

const router = express.Router();

const verificationsController = require("../../controllers/users/verificationsController");

router.get("/verify/:verificationToken", verificationsController);

module.exports = router;
